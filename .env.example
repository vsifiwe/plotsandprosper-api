# Plots & Prosper backend — copy to .env and fill in values

# Database (PostgreSQL 16)
# Option A: individual vars
DB_NAME=plots_prosper
DB_USER=plots_prosper
DB_PASSWORD=changeme
DB_HOST=localhost
DB_PORT=5432

# Option B: single URL (override if your app parses it)
# DATABASE_URL=postgres://user:pass@localhost:5432/plots_prosper

# Django
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# JWT (optional overrides)
# SIMPLE_JWT_ACCESS_TOKEN_LIFETIME=minutes=60
# SIMPLE_JWT_REFRESH_TOKEN_LIFETIME=days=7
